#include <iostream>
using namespace std;

int main(){
    cout << "DSA LAB for Git Setting up" << endl;

    //This is done so that we can get ourselves some changes done in the existing code file and push it to the repository with the commit
    cout << "This is done so that we can get ourselves some changes done in the existing code file and push it to the repository with the commit" << endl;
    
    return 0;
}

